# The only environment variable the code reads.
# Optional: absolute path to your config.yaml
# (default: config.yaml in the project root).
SECURE_RECORD_CONFIG=

# Everything else lives in config.yaml and the files it points to
# (app password, Seafile token, kid secrets). None of those values
# belong in the environment — keep them in git-ignored files.
